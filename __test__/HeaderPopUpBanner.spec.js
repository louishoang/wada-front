import React from 'react';
import HeaderPopUpBanner from '../src/components/presentations/HeaderPopUpBanner';
import renderer from 'react-test-renderer';

test('something', () => {
  expect(1+1).toBe(2);
});